# coolify-paas

Minimal PaaS control-plane stub with health, app listing, and deploy endpoints.

Run:

```bash
npm start
```

Endpoints:

- `GET /`
- `GET /health`
- `GET /apps`
- `POST /deploy`# coolify-paas

Self-hosted PaaS: Docker-based deployment scripts to manage your own VPS infrastructure and SSL.
